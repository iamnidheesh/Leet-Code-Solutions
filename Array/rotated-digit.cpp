class Solution {
public:
    int rotatedDigits(int N) {
        string s = to_string(N);
        vector <int> rotate (10,-1);
        rotate[0] = 0;
        rotate[1] = 1;
        rotate[8] = 8;
        rotate[2] = 5;
        rotate[5] = 2;
        rotate[6] = 9;
        rotate[9] = 6;
        string k = "";
        for(int i = 0;i < s.size();i++) {
        	if(rot)
        }
    }
};